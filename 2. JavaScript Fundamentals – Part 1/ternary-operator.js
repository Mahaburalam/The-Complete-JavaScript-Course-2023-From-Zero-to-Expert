const age = 31;

const permission = age > 18 ? "can travel by bike 🚲" : "can travel by bus 🚍";
console.log(permission);

// if else insted of termary operator
if(age> 18)
{
    console.log(`You can eat pizza 🍕`);
}
else
{
    console.log(`You can eat lolipop🍭`);
}